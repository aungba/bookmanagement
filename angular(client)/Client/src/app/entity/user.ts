export class User {

    id: number;
    /** declare username to store username */
    username: string;

    /** declare email to store user's email */
    email: string;

    /** declare gender to store user's gender */
    gender: Boolean;

    /** declare address to store user's phone number */
    address: String;

    /** declare groups to store user's groups */
    role: String;
}
